import { DeleteSpecializationComponent } from './delete-specialization.component'

describe('DeleteSpecializationComponent', () => {
  it('should mount', () => {
    cy.mount(DeleteSpecializationComponent)
  })
})